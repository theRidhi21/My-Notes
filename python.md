# PYTHON:
>solves complex problems in less time with fewer lines.
>multi purpose languages.
>high level language.
>huge community
>cross platform.
>large ecosystem. 
print("x"*10) : prints x for 10 times.

###### Eg:
course = "Python Programming"
print(len(course))
print(course[0])
print(course[-1])
print(course[0:3])
print(course[0:])
print(course[:3])
print(course[:])

###### Output:
12
P
g
Pyt  
Python Progrmming
Pyt
Python Programming

 ##### Escape sequences:
 1. '\'-backslash:
eg: course("abc \"def")
output: abc "def
2. '#'-hash: for comments.
eg: # print("abc")
    print("a")
output: a
3. '\n': new line.

->a=5
b=6
c=a+" " +b or c=f"{a} {b}"
print(c)
output: 5 6

a=bcd
print(a.upper())
print(a.lower())
print(a.title())
#print(a.strip()) - to remove any spaces "rstrip" for right and "lstrip" for left.
output: BCD bcd Bcd

 same goes with find and replace 
